[phases.setup]
nixPkgs = ['php83', 'nginx', 'php83Packages.composer']

[phases.install]
cmds = ['mkdir -p /var/log/nginx', 'mkdir -p /var/cache/nginx', 'mkdir -p /run/php']

[start]
cmd = 'php-fpm -F & nginx -g "daemon off;"'
